package org.iesfm.listas;

import java.util.*;

public class ListExercises {

    // Un método que dada una lista de Strings, me devuelve el primer elemento (si existe)
    List<String> list = new ArrayList<>();

    // Un método que dada una lista de Strings, me devuelve el último elemento (si existe)


    // Un método que dada una lista de String y un String, lo inserte en la PRIMERA posición


    // Un método que dada una lista de String, una posición y un String, sustituya el valor en la posición proporcionada
    // por el valor recibido.


}
